<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="?attr/colorSecondaryText"
                android:centerColor="?attr/colorPrimaryText"
                android:centerY="1.0"
                android:endColor="?attr/colorPrimaryText"
                android:angle="270"
                />
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="?attr/colorAccent"
                    android:centerColor="?attr/colorAccent"
                    android:centerY="1.0"
                    android:endColor="?attr/colorAccent"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>